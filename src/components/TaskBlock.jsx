// TaskBlock is a placeholder for future per-block UI (completion markers, drag handles, etc.)
// Currently the Tiptap editor handles all block rendering directly.
// The margin-hover completion affordance is implemented via CSS in Editor.css.
